---
cache: pip

services:
  - docker

before_install:
  - sudo apt-get install shellcheck
  - sudo pip install yamllint
  - git clone https://github.com/IBM/pattern-ci

# before_script:
#   - "./pattern-ci/tests/shellcheck-lint.sh"
#   - "./pattern-ci/tests/yaml-lint.sh incubator-openwhisk-devtools"

jobs:
  include:
    - script: "./scripts/setup-openwhisk.sh"
